"use strict" // treat all js code as new version

let name = "Sai Praneeth"
let age = 22
let bool = true
let undf;
let n = null;

console.table([name, age, bool, undf, n]);
console.table([typeof(name), typeof(age), typeof(bool), typeof(undf), typeof(n)]);

